# FEAT-003 normalization fixtures

These fixtures are deliberately small and license-clean. Archive and image
fixtures are assembled deterministically in tests from the XML and byte
fragments here so that malicious OPC relationships, compression expansion,
and format signatures can be varied without committing opaque active content.
