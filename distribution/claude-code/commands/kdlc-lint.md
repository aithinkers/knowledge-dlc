---
description: Run the governed K-DLC lint operation
---

Invoke the argument vector ["node", "distribution/claude-code/run.mjs", "lint", "--output", "json", ...hostArgumentVector] directly without a shell. Preserve every user argument as one argv element and return the exact versioned envelope. Do not infer success when `ok` is false.
